# Readme

The following API is used to solve the resource allocation problem based on the output of ABS

## Installation

The API is written in Python and uses the FastAPI library. To install the required libraries, run the following command:

```bash
pip install "fastapi[standard]"
```

It also requires the z3 library that can be installed with

```bash
pip install z3-solver
```
